# 面向 3D 打印的 Fusion 360 协作规则

通过 MCP 操作 Autodesk Fusion，制作可实际 3D 打印且可继续编辑的模型。默认中文、毫米（mm）、角度用度；以用途、尺寸、可制造性和实物效果验收。

## 按任务读取

首次进入项目读取简短的项目上下文；其余只读当前任务涉及的规则或章节，进入新领域时补读，不重复读取未变化的内容。

| 任务涉及 | 必读规则 |
|---|---|
| 项目定位、打印设备、材料、工具状态、文件约定 | [项目上下文](rules/project-context.md) |
| 初始化、接管或重整整套规则 | [初始化](rules/project-initialization.md) |
| 官方资料、未知尺寸、工艺、外部模型、GitHub skill 或 API | [资料核验](rules/research.md) |
| 任何模型设计、修改、缩放、拆件、配合或材料选择 | [打印设计](rules/printing-design.md) |
| 连接或操作 Fusion、参数和特征、导入导出、失败恢复 | [Fusion 与 MCP](rules/fusion-mcp.md) |
| Bambu Studio 操作、验证、切片、交付、试打印或失败排查 | [验证与交付](rules/testing.md) |
| 凭据、外部模型与脚本、覆盖、云端共享或实机操作 | [安全边界](rules/security.md) |
| 修改本地文件、版本保存、Git 同步 | [文件与版本管理](rules/git-workflow.md) |
| 有效经验、用户纠正、规则冲突或维护约定 | [规则维护](rules/rule-maintenance.md) |

## 任务范围与自主推进

- 项目内约定以当前用户明确要求为先，其次是更具体目录的 AGENTS.md、本文件、路由规则和既有模型惯例；项目规则不改变系统与工具权限边界。
- “研究、审查、诊断”对目标模型和应用默认只读；“修改、实现、建模”授权完成对应工作。用户明确要求“先讨论”“不改文件”“只切片”或“手动打印”时按该范围停止。
- 能从模型、工具状态、切片配置和资料确认的信息自行调查；已有设备、偏好和授权直接复用。外观及非配合尺寸等可逆选择说明假设后继续。
- 关键接口、载荷或设备信息缺失时，只暂停依赖该信息的定案或操作，继续其余结构、参数关系或小样准备；必要问题说明它影响哪项决定。
- 已授权任务持续推进至对应交付：建模完成 CAD 检查与保存；打印文件交付包含相关导出，工具具备时完成真实预设切片与路径检查。修复本次问题并复核受影响部分，无需逐步询问。等待实测或工具受阻时交付已有成果、验证边界和具体剩余项。
- 仅在需要操作 Fusion 时连接并按恢复规则处理；仅在修改或明确同步任务中执行 Git 同步。普通建模按需要使用最小辅助脚本，不搭建无关软件工程或常驻服务。

## 核心约束

- 先用途与工艺再几何；区分用户尺寸、实测、官方规格和设计假设。打印姿态同时考虑受力、落床、支撑拆除和装配。
- Fusion 原生设计与关键参数是权威来源；保护固定硬件接口，不将经验壁厚、间隙或悬垂值当作通用保证。
- 修改前确认文档与目标，按完整操作批次回读参数和几何；超时先检查是否生效。保护原始输入和未保存工作，高影响修改保留可恢复版本。
- CAD、导出、切片和实物分别验收；变更后更新并检查受影响产物，只宣称有证据支持的结果。
- 建模或切片不包含发送打印、加热、运动、校准或升级固件；具体授权及设备核对见安全边界。

## 持续维护授权

用户已授权在后续工作中主动记录有用事实、沉淀有效经验、修改不合适的规则，并评估和清理与旧规则的冲突，无需逐次征求同意。发现可复用结论或规则问题时按[规则维护](rules/rule-maintenance.md)落盘并验证；只保存会改善后续工作的内容。当前用户明确要求不改文件时，本轮仅提出建议。
