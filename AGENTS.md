# 面向 3D 打印的 Fusion 360 协作规则

本项目用于通过 MCP 操作 Autodesk Fusion（Fusion 360），制作最终要实际 3D 打印的模型。默认中文沟通、毫米（mm）标注。以用途、尺寸、可制造性和实物效果验收，不以渲染图好看或工具返回成功作为完成依据。

## 开始前按任务读取

只完整读取命中的规则；进入新领域时补读。首次进入项目必读项目上下文。

| 任务涉及 | 必读规则 |
|---|---|
| 项目定位、打印设备、材料、工具状态、文件约定 | [项目上下文](rules/project-context.md) |
| 初始化、接管或重整整套规则 | [初始化](rules/project-initialization.md) |
| 官方资料、未知尺寸、工艺、外部模型、GitHub skill 或 API | [资料核验](rules/research.md) |
| 任何模型设计、修改、缩放、拆件、配合或材料选择 | [打印设计](rules/printing-design.md) |
| 连接或操作 Fusion、参数和特征、导入导出、失败恢复 | [Fusion 与 MCP](rules/fusion-mcp.md) |
| Bambu Studio 操作、验证、切片、交付、试打印或失败排查 | [验证与交付](rules/testing.md) |
| 凭据、外部模型与脚本、覆盖、云端共享或实机操作 | [安全边界](rules/security.md) |
| 修改本地文件、版本保存、Git 同步 | [文件与版本管理](rules/git-workflow.md) |
| 用户纠正、重复问题或维护约定 | [规则维护](rules/rule-maintenance.md) |

## 指令与沟通

- 当前用户明确要求优先；随后是更具体目录的 AGENTS.md、本文件、路由规则和既有模型惯例。
- 发现影响尺寸、装配、材料或用户数据的冲突，指出影响；无法从现有证据决定时只询问必要问题，其他可逆细节自主推进。
- 能从模型、工具状态、切片配置或官方资料确认的信息自行调查。已确认的设备与偏好不重复询问。
- “研究、审查、诊断”默认只读；“修改、实现、建模”授权完成对应范围的工作。此次初始化授权改写和裁剪项目规则。
- 不为普通建模搭建软件工程、安装替代 CAD、创建常驻服务或额外报告。必要脚本仅服务于当前模型或已确认的自动化能力。

## 每次建模都要遵守

1. **先用途与工艺，再几何**：确认使用环境、关键配合、载荷方向、材料和打印姿态；所有模型都必须考虑实际打印。
2. **尺寸有出处**：区分用户尺寸、实测值、官方规格和设计假设。涉及真实硬件的关键接口缺少依据时，先解决该缺口；自由设计尺寸可合理设定并说明。
3. **保持可编辑**：以 Fusion 原生设计及关键参数为权威来源，网格和切片是派生产物。缩放不能无意改变螺丝、磁铁、轴承等固定硬件接口。
4. **打印方向影响结构**：同时考虑层间受力、支撑接触面、支撑拆除路径、首层附着和装配方向，不能只选最省支撑的朝向。
5. **经验值不冒充保证**：喷嘴直径、线宽、层高、间隙和补偿是不同参数；不得统一套用固定壁厚、公差或悬垂极限。
6. **先读再写、写后回读**：确认 Fusion 当前文档与目标实体，小步修改，核对参数、几何和特征状态。超时先检查是否已生效，再决定是否重试。
7. **保护现场**：保留用户未保存工作和原始输入，不随意重启 Fusion、清缓存、替换 MCP 或覆盖文件；高影响操作先保留可恢复版本。
8. **分层验收**：CAD 检查、导出检查、切片检查、实物验证分别记录。没有切片或实测证据时，明确停在哪一层。
9. **修改必须闭环**：尺寸、材料或姿态变更后，重新生成受影响产物并复核，不能交付旧切片或旧预览。

## 工作顺序

- 调查项目文件和工具能力；Fusion 未出现在工具列表时仍检查已确认的本地 MCP 地址，连接失败按 Fusion 规则主动启动应用、检查开关并重试。若已是 Git 仓库，按版本规则安全同步。
- 明确需求和少量关键参数，形成打印姿态、分件与配合方案。
- 在正确的 Fusion 文档中按完整特征逐步建模、测量和保存。
- 检查模型及导出文件；条件具备时使用真实机型配置切片并查看逐层路径。
- 关键配合或新工艺先做小样方案，再根据实物反馈修改。
- 交付模型位置、关键尺寸、材料与摆放建议、实际验证结果和剩余事项。仅当已有 Git 仓库且达到完整边界时提交并同步。

“要做可打印模型”不自动授权发送打印任务、加热、运动、校准或升级固件；实机授权按安全规则执行。
