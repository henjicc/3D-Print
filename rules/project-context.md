# 项目上下文

## 稳定约定

- 个人 CAD 与打印制作工作区；Fusion 原生建模，Bambu Studio 切片，中文、mm、度。
- 用户于 2026-09-10 确认：拓竹 **X2D、0.4 mm 喷嘴，以 PLA 为主，少量 PETG**。尽量一体成型；必要的清洗、维护和制造分件仍保留。
- macOS 应用：`/Users/henji/Applications/Autodesk Fusion.app`、`/Users/henji/Applications/BambuStudio.app`。
- Fusion 使用内置本地 MCP Server；设置入口为 **Fusion → 首选项 → 常规 → API → Fusion MCP Server**，默认地址 `http://127.0.0.1:27182/mcp`。已授权连接失败时启动 Fusion、检查 / 启用该开关并核对实际端口，见 [Fusion 操作](fusion-mcp.md)。
- Bambu Studio 已验证方式为官方程序命令行离线切片、既有 GUI 回开预览；未使用专用 Bambu Studio MCP。电脑控制工具接入 MCP 不等于目标应用提供原生 MCP。
- 已建立本地 Git；2026-09-12 确认仅跟踪 `AGENTS.md`、`rules/` 与 `.gitignore`。整个 `models/`、缓存和日志仅本地保留。最近检查未配置远端；同步时读取实际仓库状态。没有开发服务或构建框架。
- 2026-09-12 用户授权主动记录、经验沉淀、规则修改与冲突清理，无需逐次询问；见 [规则维护](rule-maintenance.md)。

## 最近观测：使用时复核

- Fusion：2026-09-10 版本 **2705.1.15**，协议 `2025-03-26`，适配器 `MCP Server Adapter 1.0.0`；2026-09-11 最近成功地址为 `http://127.0.0.1:62842/mcp`，经过握手、工具发现、原生模型回开及截图验证。62842 为当次运行值，重启或连接失败时读取实际 URL。
- 曾发现 `fusion_mcp_read`、`fusion_mcp_execute`、`fusion_mcp_update`、`fusion_mcp_electronics_read`；连接时以实时工具定义为准。会话未注册工具时仍可检查本地 HTTP MCP。
- Bambu Studio：2026-09-11 核验 **2.8.2.61**，安装包 SHA256 与官方值匹配，自带 X2D 0.4 mm、PLA Basic 和 SuperTack 配置。执行切片时核对当前版本、机型、喷嘴、材料系列及逐板板型；AMS 槽位和装载状态不作为长期默认。
- 当前 Fusion 版本曾发生资源耗尽空响应；同版本直接 HTTP 建模或出现同类症状时读取 [排障与临时防护](fusion-troubleshooting.md)，升级后复核适用性。
- 收纳架具体材料、修订和试打结果见本地 `models/sink-organizer/print-notes.md`，接手该模型时再读；不作为所有模型的配置。

## 设备与工艺基线

- 工艺：FDM / FFF 熔融挤出。默认按用户的 0.4 mm 喷嘴和 PLA 开展普通室内模型方案；耐热、长期受力、弹性或户外用途必须重新评估材料，不能仅沿用默认值。
- X2D 官方资料给出的主喷嘴范围为 **256 × 256 × 260 mm**，副喷嘴及双喷嘴交集为 **235.5 × 256 × 256 mm**。此处依据 2026-04-14 官方说明，核验于 2026-09-10。
- X2D 主、副喷嘴的送料与校准条件不同。官方建议主喷嘴用于模型、副喷嘴用于支撑；实际任务需核对喷嘴分工、各自口径、装载材料及可达区域。
- 上述范围不是无条件可用的最大模型尺寸。切片时按当前 X2D 配置、禁区、支撑、裙边 / brim、擦拭塔和喷嘴分配检查；不能把双喷嘴并集当成两支喷嘴均可访问的区域。
- 层高、线宽、温度、速度、壁层、填充和支撑从当前机型、喷嘴、耗材及打印板的真实预设出发，按模型需要调整。当前没有实测的配合间隙或收缩补偿基线。


## 文件约定

- AGENTS.md 是入口；rules/ 保存按需读取的长期规则和资料入口。
- 模型需要落盘时再建立 `models/<模型名称>/`，不预建空目录树。
- Fusion 原生文档保存参数、特征与组件；需要本地归档时保存适用的 F3D / F3Z。STEP 用于几何交换，不能代替完整的原生历史。
- 打印网格优先 3MF，接口不支持时可导出二进制 STL 并明确 mm；通过目标切片器验证。按需保存 Bambu Studio 项目 3MF，区分它和纯几何 3MF。
- 同一模型的源文件、导出件、切片项目及必要的实际预览放在模型目录中，用清晰的名称和修订号对应；不存在的产物不预留空文件。
- 多次试打印时在该模型目录维护一份简短 `print-notes.md`，仅记录源文档 / 修订、关键参数、材料与预设、姿态、验证状态、实测结果及有效修改；不另建数据库或通用日志系统。
- 私有模型、账号数据和设备凭据不得自动发布；本地路径与云端文档位置按任务需要引用。

## 按需资料

设备、工艺、API 和格式的一手来源及历史第三方调研见 [资料入口](references.md)。每个模型复用已有用途和尺寸信息；双材料任务另核对主副喷嘴分配、可达范围、送料状态和材料相容性。
